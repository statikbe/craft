{% if socialMedia.facebook %}
    <li class="">
        <a href="{{ socialMedia.facebook }}" aria-label="{{ 'Bekijk onze Facebook pagina'|t }}" target="_blank" rel="noopener" class="inline-block text-primary hover:text-secondary nav__link--facebook">
            {% include "_site/_snippet/_global/_icon-sprite" with {baseUrl: alias("@baseUrl"), slug: "facebook", class: ""} %}
            <span class="sr-only">{{ 'Facebook'|t }}</span>
        </a>
    </li>
{% endif %}
{% if socialMedia.instagram %}
    <li class="">
        <a href="{{ socialMedia.instagram }}" aria-label="{{ 'Bekijk onze Instagram pagina'|t }}" target="_blank" rel="noopener" class="inline-block text-primary hover:text-secondary nav__link--instagram">
            {% include "_site/_snippet/_global/_icon-sprite" with {baseUrl: alias("@baseUrl"), slug: "instagram", class: ""} %}
            <span class="sr-only">{{ 'Instagram'|t }}</span>
        </a>
    </li>
{% endif %}
{% if socialMedia.linkedin %}
    <li class="">
        <a href="{{ socialMedia.linkedin }}" aria-label="{{ 'Bekijk onze LinkedIn pagina'|t }}" target="_blank" rel="noopener" class="inline-block text-primary hover:text-secondary nav__link--linkedin">
            {% include "_site/_snippet/_global/_icon-sprite" with {baseUrl: alias("@baseUrl"), slug: "linkedin", class: ""} %}
            <span class="sr-only">{{ 'LinkedIn'|t }}</span>
        </a>
    </li>
{% endif %}
{% if socialMedia.twitter %}
    <li class="">
        <a href="{{ socialMedia.twitter }}" aria-label="{{ 'Bekijk onze Twitter pagina'|t }}" target="_blank" rel="noopener" class="inline-block text-primary hover:text-secondary nav__link--twitter">
            {% include "_site/_snippet/_global/_icon-sprite" with {baseUrl: alias("@baseUrl"), slug: "twitter", class: ""} %}
            <span class="sr-only">{{ 'Twitter'|t }}</span>
        </a>
    </li>
{% endif %}
{% if socialMedia.youtube %}
    <li class="">
        <a href="{{ socialMedia.youtube }}" aria-label="{{ 'Bekijk onze YouTube pagina'|t }}" target="_blank" rel="noopener" class="inline-block text-primary hover:text-secondary nav__link--youtube">
            {% include "_site/_snippet/_global/_icon-sprite" with {baseUrl: alias("@baseUrl"), slug: "youtube", class: ""} %}
            <span class="sr-only">{{ 'YouTube'|t }}</span>
        </a>
    </li>
{% endif %}
